name: git actions demo
on:
  - push
jobs:
  hello-world:
   runs-on: ubuntu-latest
   steps:
    - run: echo "hello SRE"
    - name: checkout
      uses: actions/checkout@v4
    - name: list files
      run: ls -a
